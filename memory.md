# AGENTS.md — CSE303 Project Memory

## What this file is
Persistent memory for the coding agent (opencode). It records what has been done for the
Heart Disease project and what is still left to do. Read it at the start of every session.

## Project overview
- **Course:** Statistics for Data Science (CSE303), 3rd-year undergraduate, taught in Python.
- **Student/team:** user is "Ratul". Other teammates have their own notebooks outside the project folder.
- **Dataset:** `heart_disease_risk_2026.csv` — synthetic dataset from Kaggle
  ("Heart Disease Risk 2026", by srisyra02). **9000 rows, 27 columns.** No missing values, no duplicates.
- **Target variable:** `has_heart_disease` (0/1). `0` = no disease (6273), `1` = disease (2727) → imbalanced (30.3% positive).
- **Git repo:** https://github.com/nirjon001/CSE303_Initial_final_project (private), branch `main`.

## File locations (all in this repo's root)
- **Notebook (single deliverable, 8 parts):** `Heart_disease_Analysis_Group-7(Updated).ipynb`
- **CSV (must stay in same folder as notebook):** `heart_disease_risk_2026.csv`
- **Text report (EDA):** `report.txt` — covers Parts 1-7.
- **Text report (modeling):** `modeling_report.txt` — covers Part 8.
- **Instructions PDFs:** `CSE303_GroupProject Initial_Instructions .pdf` (initial) and
  `CSE303_GroupProject_Instructions (2).pdf` (final). Final instructions require ONE merged notebook
  containing both the EDA and the predictive modeling.
- **Teammates' notebooks (style references, NOT in this repo):** `Ratul_Heart_analysis.ipynb` (Code 1),
  `notebook_303_Mridul.ipynb` (Code 2), `Heart_disease_risk-1_Labib.ipynb` (Code 3),
  `Heart_disease_risk_303-16-20.ipynb`, `Heart_disease_risk_303_Columns_21_24.ipynb`, `197.K.ipynb`.
- The old standalone `Heart_disease_Modeling_Group-7.ipynb` was **deleted** (merged into Part 8).
  The old `build_notebook2.py` / `Heart_disease_merged.ipynb` workflow is **obsolete** — do not reuse.

## Environment
- **Python interpreter:** `C:\Program Files\Python314\python.exe` (system Python 3.14, has
  pandas/numpy/matplotlib/seaborn/sklearn/nbformat/nbclient/nbconvert/ipykernel).
- **Jupyter kernel registered:** `cse303` → points at this interpreter
  (`C:\Users\nirjo\AppData\Roaming\jupyter\kernels\cse303\kernel.json`). Re-registered Aug 2026 after
  the old venv paths (`D:\GitHub_Repository_Destination\...` and `D:\Coding-Arena\CSE303-project\.venv`)
  went stale or lost sklearn. Tell user to select the `cse303` kernel in VSCode.
- Do NOT use `cse303-venv` / `python3` kernels: they point to `D:\Coding-Arena\CSE303-project\.venv`
  which is MISSING scikit-learn.
- **Run the whole notebook headless to verify:** copy CSV into a temp dir next to the notebook, then
  `& ".venv\Scripts\python.exe" -m jupyter nbconvert --to notebook --execute --ExecutePreprocessor.timeout=600 "<temp>\<nb>.ipynb" --output "<nb>_executed.ipynb" --output-dir <temp>`.

## How the notebook is edited (current workflow, IMPORTANT)
The notebook is **not** generated by a builder script anymore. It is edited directly:
1. Apply changes with a small Python transform script in `C:\Users\nirjo\AppData\Local\Temp\opencode\`
   (read with `nbformat`, rewrite cell sources, write to a temp `.ipynb`).
2. Copy `heart_disease_risk_2026.csv` into the temp dir (notebook reads it with a relative path).
3. Execute headless via nbconvert (kernel `cse303`), verify **0 error cells** with
   `nbformat.read(...)` checking `output_type == "error"`.
4. `Copy-Item` the executed temp notebook back over `Heart_disease_Analysis_Group-7(Updated).ipynb`
   (saves outputs inline so results show immediately in VSCode).
5. Only commit/push when the user explicitly asks.

## COMPLETED work (current notebook = 124 cells, 72 code + 52 markdown) — ALL 8 PARTS DONE
### Part 1 — Dataset Description ✅
- Source & real-world problem (Kaggle synthetic, heart-disease risk).
- 9000 rows × 27 columns; all 27 features described in a table (dtype, meaning, classification).
- 19 numerical features vs 7 categorical features; target is binary.

### Part 2 — Central Tendency and Dispersion ✅
- Table of mean, median, mode, variance, std, Q1, Q3, IQR for all 19 numerical features.
- Interpretation with real numbers (age symmetric ≈54, BP/cholesterol/BMI near clinical
  boundaries, right-skewed lifestyle features, imbalanced target).

### Part 3 — Distribution and Shape ✅
- One code cell per numerical feature (19): calls helper `plot_distribution(col)` then `print_interpretation(...)`.
- **1×3 graph** per feature: [Histogram | Density plot | horizontal Boxplot], mean (crimson dashed) and
  median (darkgreen dashed) vertical lines, skewness annotated on the histogram.
- Shape rule: |skew| < 0.5 → **Symmetric**; skew > 0.5 → Right-Skewed; skew < -0.5 → Left-Skewed.
  Mean–median note must agree with skewness-based shape.
- Interpretation per feature: Shape (skewness value), Mean, Median + diff, IQR, outlier bounds
  (Q1−1.5·IQR, Q3+1.5·IQR), outlier count + %, clinical comment. Summary table at the end.
- Key findings: 17/19 features symmetric; only `st_depression` (skew ≈ 2.04, 5.4% outliers)
  and `alcohol_units_per_week` (skew ≈ 2.03, 4.8% outliers) are right-skewed.

### Part 4 — Categorical Variable Analysis ✅
- One code cell per categorical feature (7): `plot_categorical(col)` then `print_categorical(...)`.
- Per feature: frequency table (count + %) + bar chart (counts labelled) + pie chart.
- True/False and 0/1 columns shown as Yes/No via `label_map`.
- Key findings: sex & wearable_owner nearly balanced; chest_pain_type (Asymptomatic 47.4% dominates),
  exercise_induced_angina, family_history, smoker_status imbalanced; target imbalanced (30.3% positive).

### Part 5 — Relationships Between Variables ✅
- Prep cell adds `df["hd_label"]` for readable legends.
- 10 relationship plots + interpretation (scatterplots with hue=disease, boxplot/countplot vs target,
  Pearson r per pair). Key numbers:
  1. `age` vs `cholesterol_total` scatter (r = 0.128, weak).
  2. Same split by `sex` (Male disease rate 35.4% vs Female 24.7%).
  3. `hdl` vs `ldl` (r = -0.082); disease group LOWER HDL (50.5 vs 57.3), HIGHER LDL (114.4 vs 98.5).
  4. `age` vs `bmi` split by sex (r = 0.039).
  5. `bmi` vs disease boxplot (26.7 vs 24.7, r = 0.213).
  6. `chest_pain_type` vs disease: Typical Angina highest 47.9%, Asymptomatic lowest 25.4%.
  7. `fasting_blood_sugar` vs disease (126.5 vs 116.4, r = 0.202).
  8. `family_history` vs disease (35.6% vs 28.3%).
  9. systolic/diastolic BP vs disease, 1×2 boxplots (r = 0.249 / 0.193).
  10. `smoker_status` × `alcohol_units_per_week` × disease: Current smokers 46.4%, alcohol r ≈ 0.04 (weak).
- **5.12 Correlation matrix** (20x20: 19 numerical + target), `sns.heatmap(annot=True, fmt=".2f", cmap="coolwarm")`.
  "Reading the matrix" cell also cites **age ↔ max_heart_rate_achieved r = -0.73** (backs up Part 8's
  claim in the LR setup cell).

### Part 6 — Data Quality Assessment ✅
- 5 checks: missing values (0), duplicate rows (0), min/max per numeric column, sensible-range
  bounds check (clinical ranges), categorical unexpected-categories check.
- Discussion: handling if problems existed (drop/impute/remove); Part 3 IQR outliers are real extreme
  patients (keep). Conclusion: dataset clean, no preprocessing needed.

### Part 7 — Key Insights ✅
- Code cell prints the strongest 8 numeric predictors (by |r|) + disease-rate table for key groups.
- Markdown: (7.1) patient risk profile table, (7.2) strongest signals (max_heart_rate r=-0.58,
  st_depression r=+0.36, age, ldl, hdl, systolic BP), (7.3) categorical/lifestyle insights,
  (7.4) variables to keep vs weakest + imbalance warning, (7.5) final conclusion pointing to Part 8.

### Part 8 — Predictive Modeling ✅ (heading: "# Part 8: Predictive Modeling")
- Setup cell re-imports libraries and re-reads the CSV (cell 94). This is a KNOWN duplicate of cells 2/5;
  the user chose to keep it. Do NOT remove unless asked.
- `numerical_cols`/`categorical_cols` redefined (same as Part 1), `pd.get_dummies(drop_first=True)` for
  categories → feature table X; `y = has_heart_disease`. Train/test split 80/20, `random_state=42`,
  `stratify=y` for classification.
- **8.3 Linear Regression** (target `max_heart_rate_achieved`, feature `age`):
  - The manual normal-equation model is defined **inline** in the LR cell (NOT a separate file):
    `linear_regression_manual(X, y)` using `np.linalg.inv` on `X.T @ X` step-by-step, plus
    `predict_linear(X, beta)`. User's own implementation, keep as-is.
  - Same cell fits sklearn `LinearRegression` and prints a comparison table — manual and sklearn match
    EXACTLY: intercept 229.6355, slope -1.2006, MSE 216.43, RMSE 14.71, R² 0.524.
  - Instructor requires Linear Regression done BOTH manually and with sklearn. **Only LR needs the
    manual comparison** — logistic/kNN/DT/RF/NB stay as imported sklearn models.
  - Multiple regression (6 features: age, bmi, resting_bp_systolic, hba1c, resting_heart_rate, sex_Male):
    MSE 207.17, RMSE 14.39, R² 0.545. All-features (max_heart_rate_achieved removed first — data leakage
    warning): MSE 185.02, RMSE 13.60, R² 0.593.
  - The 6-feature and all-features MANUAL cells (106 and 111) are **self-contained**: they rebuild their
    own train/test data, define their own `normal_equation`/`predict_many` helpers, and re-fit sklearn,
    so they run with NO NameError even if cells 101/105/110 were not run. They still need the Part 8
    setup cells (94-99). Verified with nbclient: full notebook = 0 error cells, and cells 106+111 run
    in isolation (94-99 + 106,111) = 0 error cells.
- **8.4 Logistic Regression** (scaled with StandardScaler, learned on training set only):
  Acc 0.8972, Prec 0.8614, Rec 0.7872, F1 0.8226, AUC 0.9497, CM [[1186,69],[116,429]].
- **8.5 Additional models** (same split): kNN(5) 0.855/0.860/0.622/0.722, DecisionTree(depth 5)
  0.854/0.800/0.692/0.742, RandomForest 0.881/0.853/0.732/0.788, NaiveBayes 0.870/0.769/0.817/0.792.
  Bar chart comparing Accuracy/F1 + side-by-side confusion matrices.
- **8.6 Results and Discussion**, **8.7 Conclusions**. Key story: data nearly linearly separable, so
  logistic wins over more complex models; evaluate with F1/ROC-AUC, not plain accuracy.

## Style / convention rules the user wants (IMPORTANT — follow these next time)
- **Beginner-friendly code**: plain loops, heavy comments, no clever one-liners, no sklearn/pipelines.
- Plots for numeric features are **1 row × 3 columns** (Code 1 style): histogram, density plot, boxplot.
- **Mean & median lines must be visible** on the plots.
- **Skewness must be reported both as a label and a number**, e.g. "Left-Skewed, skewness = 0.092".
- Interpretation style = **mix of Code 1 + Code 3**: IQR bounds/outlier counts PLUS a meaningful
  clinical/statistical comment per feature.
- Categorical variables: pie charts and bar charts "as you see fit".
- Keep sections ordered exactly like the assignment (Parts 1-8).
- Code must look like a 3rd-year undergrad wrote it: **no docstrings** (use `#` comments instead),
  **no aligned `=` blocks**, banner comments (e.g. `# ===== AGE =====`) kept, substantive "why" comments
  kept, narration trimmed. (This was an explicit de-AI pass in Aug 2026.)
- Keep the notebook text student-like: NO em-dashes, no AI-flavoured filler phrases, no stale
  "next steps" text. Use plain "Part N: Name" headings.
- The user's process is: finish a part → verify it runs → report results and next step. Don't skip ahead.

## Gotchas / reminders
- When generating notebook cells with Python scripts, do NOT use `"""` as the cell string delimiter if the
  cell contains Python docstrings — use `'''` for those cells instead. (Some Part 8 code cells DO contain
  docstrings, e.g. the inline manual LR.)
- Boxplot: use `orientation='horizontal'` (not `vert=False`) to avoid a Matplotlib deprecation warning.
- The user will open the notebook in VSCode; select the `cse303` kernel. If VSCode says the file is
  "newer on disk", tell them to click **Revert File** (never "Overwrite").
- `chat.json` and `.venv/` are gitignored; do not stage them.

## Historical notes (Aug 2026)
- **Smart App Control blocker RESOLVED:** Windows SAC was blocking pandas' `_libs/window/aggregations.cp314
  -win_amd64.pyd`, breaking `import pandas`. User turned SAC **Off**. If it is turned back **On**, pandas
  gets blocked again — must be **Off** or **Evaluation** before running the notebook.
- **VSCode "Run All" fix:** kernel spec must be `cse303` (venv), not `python3`, or VSCode may silently fail
  to start the kernel. Notebook is saved with outputs inline.
- Headings were renumbered: modeling section used to be `## 8.2.x`; now clean `8.1`–`8.7` under `# Part 8`.
- Cell 0 title now reads "Exploratory Data Analysis and Predictive Modeling" and lists all 8 parts.